[project]
name = "luisa-render-scenes"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "imageio",
    "scikit-image",
    "opencv-python",
    "matplotlib>=3.10.9",
    "pandas>=3.0.2",
]

[tool.uv]
dev-dependencies = []
